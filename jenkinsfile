pipeline {
    agent {
        node {
            label "linux && java11"
        }
    }
    stages {

        stage ("build") {
            steps {
                echo("hello build 1")
                echo("hello build 2")
                echo("hello build 3")
            }
        }

        stage ("test") {
            steps {
                echo("hello test")
            }
        }

        stage ("deploy") {
            steps {
                echo("hello deploy")
            }
        }
    }
    post {
        always {
            echo "1 will always say hello riannnnnn"
        }
        success {
            echo "yeay,success"
        }
        failure {
            echo "oh no, failure"
        }
        cleanup {
            echo "don't care success or error"
        }
    }
}